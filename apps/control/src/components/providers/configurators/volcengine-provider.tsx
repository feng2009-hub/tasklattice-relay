import type { ProviderConnectionDraft } from "@tasklattice/contracts";
import { ProviderFormSection, ProviderTextField } from "./fields";
import type { ProviderConfiguratorProps } from "./types";
type Draft = Extract<ProviderConnectionDraft, { provider: "volcengine" }>;
export function VolcengineProvider({ disabled, errors, onChange, value }: ProviderConfiguratorProps<Draft>) { return <ProviderFormSection title="Volcengine Ark deployment" description="Volcengine routes chat requests by Endpoint ID rather than a public catalog model name."><ProviderTextField id="volcengine-endpoint-id" label="Endpoint ID" required value={value.config.endpointId} disabled={disabled} error={errors.endpointId} onChange={(endpointId) => onChange({ ...value, config: { ...value.config, endpointId } })} /><ProviderTextField id="volcengine-endpoint" label="API endpoint" type="url" required value={value.config.endpoint} disabled={disabled} onChange={(endpoint) => onChange({ ...value, config: { ...value.config, endpoint } })} /><ProviderTextField id="volcengine-key" label="API key" type="password" required value={value.credentials.apiKey} disabled={disabled} error={errors.apiKey} onChange={(apiKey) => onChange({ ...value, credentials: { apiKey } })} /></ProviderFormSection>; }
