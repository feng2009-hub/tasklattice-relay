import type { ProviderConnectionDraft } from "@tasklattice/contracts";
import { ProviderFormSection, ProviderTextField } from "./fields";
import type { ProviderConfiguratorProps } from "./types";
type Draft = Extract<ProviderConnectionDraft, { provider: "azure-openai" }>;
export function AzureOpenAIProvider({ disabled, errors, onChange, value }: ProviderConfiguratorProps<Draft>) { return <ProviderFormSection title="Azure OpenAI deployment"><ProviderTextField id="azure-endpoint" label="Azure endpoint" type="url" required placeholder="https://resource.openai.azure.com" value={value.config.endpoint} disabled={disabled} error={errors.endpoint} onChange={(endpoint) => onChange({ ...value, config: { ...value.config, endpoint } })} /><div className="grid gap-4 sm:grid-cols-2"><ProviderTextField id="azure-version" label="API version" required value={value.config.apiVersion} disabled={disabled} onChange={(apiVersion) => onChange({ ...value, config: { ...value.config, apiVersion } })} /><ProviderTextField id="azure-deployment" label="Deployment name" required value={value.config.deployment} disabled={disabled} error={errors.deployment} onChange={(deployment) => onChange({ ...value, config: { ...value.config, deployment } })} /></div><ProviderTextField id="azure-key" label="API key" type="password" required value={value.credentials.apiKey} disabled={disabled} error={errors.apiKey} onChange={(apiKey) => onChange({ ...value, credentials: { apiKey } })} /></ProviderFormSection>; }
