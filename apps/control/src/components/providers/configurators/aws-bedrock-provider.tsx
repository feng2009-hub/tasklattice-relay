import type { ProviderConnectionDraft } from "@tasklattice/contracts";
import { ProviderFormSection, ProviderTextField } from "./fields";
import type { ProviderConfiguratorProps } from "./types";
type Draft = Extract<ProviderConnectionDraft, { provider: "aws-bedrock" }>;
export function AwsBedrockProvider({ disabled, errors, onChange, value }: ProviderConfiguratorProps<Draft>) { return <ProviderFormSection title="AWS Bedrock credentials"><div className="grid gap-4 sm:grid-cols-2"><ProviderTextField id="bedrock-region" label="AWS region" required value={value.config.region} disabled={disabled} error={errors.region} onChange={(region) => onChange({ ...value, config: { ...value.config, region } })} /><ProviderTextField id="bedrock-role" label="Role ARN (optional)" value={value.config.roleArn ?? ""} disabled={disabled} onChange={(roleArn) => onChange({ ...value, config: { ...value.config, roleArn: roleArn || undefined } })} /></div><ProviderTextField id="bedrock-access-key" label="Access key ID" type="password" required value={value.credentials.accessKeyId} disabled={disabled} error={errors.accessKeyId} onChange={(accessKeyId) => onChange({ ...value, credentials: { ...value.credentials, accessKeyId } })} /><ProviderTextField id="bedrock-secret-key" label="Secret access key" type="password" required value={value.credentials.secretAccessKey} disabled={disabled} error={errors.secretAccessKey} onChange={(secretAccessKey) => onChange({ ...value, credentials: { ...value.credentials, secretAccessKey } })} /><ProviderTextField id="bedrock-session-token" label="Session token (optional)" type="password" value={value.credentials.sessionToken ?? ""} disabled={disabled} onChange={(sessionToken) => onChange({ ...value, credentials: { ...value.credentials, sessionToken: sessionToken || undefined } })} /></ProviderFormSection>; }
