import type { ProviderConnectionDraft } from "@tasklattice/contracts";
import { ProviderFormSection, ProviderSelectField, ProviderTextField } from "./fields";
import type { ProviderConfiguratorProps } from "./types";
type Draft = Extract<ProviderConnectionDraft, { provider: "qwen" }>;
const endpoints = { cn: "https://dashscope.aliyuncs.com/compatible-mode/v1", international: "https://dashscope-intl.aliyuncs.com/compatible-mode/v1" } as const;
export function QwenProvider({ disabled, errors, onChange, value }: ProviderConfiguratorProps<Draft>) { return <ProviderFormSection title="DashScope connection"><ProviderSelectField id="qwen-region" label="Region" value={value.config.region} disabled={disabled} options={[{ value: "cn", label: "Mainland China" }, { value: "international", label: "International" }]} onChange={(region) => onChange({ ...value, config: { region: region as Draft["config"]["region"], endpoint: endpoints[region as keyof typeof endpoints] } })} /><ProviderTextField id="qwen-endpoint" label="API endpoint" type="url" required value={value.config.endpoint} disabled={disabled} error={errors.endpoint} onChange={(endpoint) => onChange({ ...value, config: { ...value.config, endpoint } })} /><ProviderTextField id="qwen-key" label="API key" type="password" required value={value.credentials.apiKey} disabled={disabled} error={errors.apiKey} onChange={(apiKey) => onChange({ ...value, credentials: { apiKey } })} /></ProviderFormSection>; }
