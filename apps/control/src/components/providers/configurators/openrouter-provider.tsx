import type { ProviderConnectionDraft } from "@tasklattice/contracts";
import { ProviderFormSection, ProviderTextField } from "./fields";
import type { ProviderConfiguratorProps } from "./types";
type Draft = Extract<ProviderConnectionDraft, { provider: "openrouter" }>;
export function OpenRouterProvider({ disabled, errors, onChange, value }: ProviderConfiguratorProps<Draft>) { return <ProviderFormSection title="OpenRouter connection"><ProviderTextField id="openrouter-endpoint" label="API endpoint" type="url" required value={value.config.endpoint} disabled={disabled} onChange={(endpoint) => onChange({ ...value, config: { ...value.config, endpoint } })} /><div className="grid gap-4 sm:grid-cols-2"><ProviderTextField id="openrouter-site" label="Site URL (optional)" type="url" value={value.config.siteUrl ?? ""} disabled={disabled} onChange={(siteUrl) => onChange({ ...value, config: { ...value.config, siteUrl: siteUrl || undefined } })} /><ProviderTextField id="openrouter-app" label="App name (optional)" value={value.config.appName ?? ""} disabled={disabled} onChange={(appName) => onChange({ ...value, config: { ...value.config, appName: appName || undefined } })} /></div><ProviderTextField id="openrouter-key" label="API key" type="password" required value={value.credentials.apiKey} disabled={disabled} error={errors.apiKey} onChange={(apiKey) => onChange({ ...value, credentials: { apiKey } })} /></ProviderFormSection>; }
