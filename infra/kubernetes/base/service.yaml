apiVersion: v1
kind: Service
metadata:
  name: tasklattice-control
  namespace: tasklattice-sandboxes
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: tasklattice-control
  ports:
    - name: http
      port: 80
      targetPort: http
